# logfile-404-summary

A Makefile that bundles some scripts to generate a daily summary csv file about 404 errors in nginx server logs.

See `Makefile` for things you can do. 

Depends on [q](http://harelba.github.io/q/). You can install it on Ubuntu as `sudo apt install python3-q-text-as-data`.


